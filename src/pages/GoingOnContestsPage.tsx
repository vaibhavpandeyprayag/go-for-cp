import { FC, memo } from "react";

interface Props {}

const goingOnContestsPage: FC<Props> = (props) => {
  return <div className=""></div>;
};

goingOnContestsPage.defaultProps = {};

export default memo(goingOnContestsPage);
