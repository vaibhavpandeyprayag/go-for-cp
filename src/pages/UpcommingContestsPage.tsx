import { FC, memo } from "react";

interface Props {}

const upcommingContestsPage: FC<Props> = (props) => {
  return <div className=""></div>;
};

upcommingContestsPage.defaultProps = {};

export default memo(upcommingContestsPage);
